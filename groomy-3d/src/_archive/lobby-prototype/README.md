# lobby-prototype (archived)

런타임에 연결되지 않는 초기 로비 스케치.

- 현재 로비 3D: `src/scenes/Lobby.jsx` + `src/PlayRoot.jsx` (App에는 아직 미연결)
- 현재 제품 인트로: `src/intro/`

복구할 때 import 경로만 `../player/FirstPerson.jsx` → `./FirstPerson.jsx` 로 고치면 된다.
